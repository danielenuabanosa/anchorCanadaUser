# Provider portal — local environment
#
# Live API mode is the default. Only enable static mode for UI demos without a backend.

NEXT_PUBLIC_STATIC_MODE=false
NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Landing / marketing site (logout redirect)
NEXT_PUBLIC_LANDING_URL=http://localhost:3003
