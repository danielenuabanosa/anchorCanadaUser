# Provider portal — local environment
#
# Offline / demo auth is ON while the backend is not ready.
# Set NEXT_PUBLIC_STATIC_MODE=false once auth APIs are live.

NEXT_PUBLIC_STATIC_MODE=true
NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_APP_URL=http://localhost:3000
