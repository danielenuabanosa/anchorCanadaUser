# Figma API token for asset export (optional)
# Get one at: https://www.figma.com/developers/api#access-tokens
FIGMA_ACCESS_TOKEN=

# Set to true to browse all pages without the backend running
NEXT_PUBLIC_STATIC_MODE=false

# Disable real authentication — any dummy email/password grants access
NEXT_PUBLIC_DISABLE_AUTH=false
