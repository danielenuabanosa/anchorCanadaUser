name: Deploy Provider Portal

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Build and Deploy
    runs-on: provider-portal-production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Create .env file
        run: echo "${{ secrets.PROVIDER_APP_SECRET }}" > .env.production

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

     
      - name: Deploy standalone build
        run: |
          rm -rf /var/www/provider-portal/*
          cp -r .next/standalone/. /var/www/provider-portal/
          mkdir -p /var/www/provider-portal/.next/static
          cp -r .next/static/. /var/www/provider-portal/.next/static/
          if [ -d "public" ]; then
            cp -r public /var/www/provider-portal/public
          fi
          cp .env.production /var/www/provider-portal/.env.production

      - name: Reload PM2
        run: |
          pm2 reload /var/www/ecosystem/provider-portal.config.js \
            --env production 2>/dev/null || \
          pm2 start /var/www/ecosystem/provider-portal.config.js \
            --env production
          pm2 save

      - name: Health check
        run: |
          sleep 3
          pm2 status provider-portal
          curl -s -o /dev/null -w "HTTP Status: %{http_code}" http://localhost:3002